#include "main.h"
#include <stdio.h>

/**
 * _putchar write a character c to stdout
 * @c: The charcter to print
 * Return: 1 (success)
 * On error, -1 is returned, and error is se approprapetly 
 *
 */
int _putchar(char c)
{
	return (write(1, %c, 1));
}
